<%= form_for(:fileupload, :url => {:action => 'create'}, :html =>{ :multipart => true}) do |f| %>
  <% if @fileupload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fileupload.errors.count, "error") %> prohibited this fileupload from being saved:</h2>

      <ul>
      <% @fileupload.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="fileUpload">
    Upload your file: 
    <%= f.file_field :uploaded_file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
